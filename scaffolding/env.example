# Start setup env
# This file is an example. Copy to .env and adjust as needed.

# LM Studio Configuration
LM_STUDIO_URL="http://localhost:1234"
# The identifier of the model currently loaded in LM Studio
# You can find this in the LM Studio "Local Server" tab
DEFAULT_MODEL_ID="local-model-id"

# MCP Configuration
# Directory to allow file system access for the filesystem MCP server
MCP_FILESYSTEM_ROOT="./"

# Swarm Configuration
LOG_LEVEL="info"
